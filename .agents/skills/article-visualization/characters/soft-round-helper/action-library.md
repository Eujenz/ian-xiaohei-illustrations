# Action Library

- `dispatch`: dispatches small helper agents from a tiny task desk.
- `sort`: sorts papers, tokens, or task cards into calm blue-white piles.
- `connect`: connects task rails, inboxes, or small system pipes.
- `inspect`: inspects an exception marker or task output with a tiny pencil.
- `build`: builds a tiny working prototype from paper cards and soft machine parts.
