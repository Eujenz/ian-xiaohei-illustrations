# Article Text QA

Article text QA is deterministic in v0.1. It checks overlay JSON for non-empty text, label length, coordinate bounds, and simple suspicious simplified/traditional mixing warnings. It does not load images or use OCR.
